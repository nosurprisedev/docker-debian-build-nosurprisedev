#!/bin/bash

set -eo pipefail

source bin/set-variables

echo '##############################################'
echo "BEGIN: Creating the Docker image $DOCKER_IMAGE"
echo '##############################################'

cd "$DIR_BUILD" && docker build -t $DOCKER_IMAGE .

echo '#################################################'
echo "FINISHED: Creating the Docker image $DOCKER_IMAGE"
echo '#################################################'
